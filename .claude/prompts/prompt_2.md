You are a senior fullstack developer reviewing a hardware store management
system (Stockmaster) with a FastAPI backend and a frontend (check the
frontend folder for the framework being used).

## TASK 1 — Frontend analysis
- Read all frontend files: components, pages, services, API calls, types/interfaces
- List every API call the frontend makes: URL, HTTP method, request body,
  expected response
- Identify missing screens or features that would be expected in a hardware
  store system
- Identify any hardcoded data that should come from the API

## TASK 2 — Create a single API contract file
Create a file `frontend/src/services/api.ts` (or equivalent for the framework):
- Centralize ALL backend API calls in this single file
- Base URL should come from an environment variable
- Every function must have proper TypeScript types (or JSDoc if not TypeScript)
- Group functions by domain: auth, produtos, categorias, estoque, dashboard,
  fornecedores, vendas, clientes (based on what backend exposes)

## TASK 3 — Validate and adjust contracts
For each endpoint, decide and implement:
- Which fields should be filled by the USER (show in form)
- Which fields should be AUTO-GENERATED by the backend (hide from form)
  Examples:
    - SKU: auto-generated, do not show in create form
    - ID: auto-generated, never in create form
    - created_at / updated_at: auto-generated, show only in details
    - current_stock: managed by stock movements, not editable directly
- Adjust frontend forms and types to reflect these decisions
- If a backend endpoint is missing for something the frontend needs,
  document it in a file called `BACKEND_TODO.md` at the project root

## TASK 4 — Connection validation
- Make sure every frontend API call matches exactly the backend route,
  method and expected payload
- Fix any mismatches found
- Ensure authentication token is sent in all protected requests

## IMPORTANT
- Show me a full plan before making any changes
- Wait for my confirmation before applying
- Prioritize making the system functional end-to-end
- If something is unclear, make a decision and document it in a
  `DECISIONS.md` file at the project root